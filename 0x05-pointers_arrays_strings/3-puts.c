#include "main.h"

/**
 * main - Prints a string, followed by a new line, to stdout
 * @str: The string
 */
int main(void)
{
	char *str ;

	str = "C is fun! I love it.";
	_puts(str);
	_puts('\n');
	return (0);
}
